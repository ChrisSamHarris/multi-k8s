# Govern or manage access to our API 
apiVersion: v1
kind: Service
metadata:
  name: server-cluster-ip-service
spec:
  type: ClusterIP
  selector: 
    component: Server
  ports:
    - port: 5000
      targetPort: 5000
